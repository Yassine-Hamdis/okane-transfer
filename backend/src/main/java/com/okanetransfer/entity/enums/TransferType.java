package com.okanetransfer.entity.enums;

public enum TransferType {
    STANDARD,       // Normal cash pickup at agency
    EXPRESS,        // Faster processing (higher fee)
    MOBILE_MONEY    // Sent directly to mobile wallet
}